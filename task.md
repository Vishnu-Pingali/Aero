# Aircraft Marker System — Tasks

- [x] Research existing codebase (MapView.jsx, airlabs.js, useFlights.js)
- [x] Create `aircraftRegistry.js` — SVG silhouettes + category mapping
- [x] Update `MapView.jsx` — import registry, remove inline SVGs/lookup
- [x] Update `index.css` — 40px marker, 38px SVG, overflow: visible
- [x] Update `MapView.jsx` — Leaflet iconSize [40,40], iconAnchor [20,20]
- [x] Update `airlabs.js` — add `_enrichAircraftType()` and `_fetchAircraftDatabase()`
- [ ] Manual verification of different silhouettes rendering correctly
